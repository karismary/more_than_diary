# AI 日记助手（Diary RAG）设计文档

> 一句话描述：写日记 → 向量化存储 → 用自然语言找回记忆。

## 1. 项目定位

Diary RAG 是一个带 AI 对话能力的本地日记工具。用户写下的每篇日记都会被切分、向量化并存入本地 MySQL；之后用户可以直接用自然语言提问，系统会从历史日记中检索相关内容，再交给 LLM 生成带出处的回答。

当前阶段以**本地网页**为主，先跑通完整体验，后续有机会再打包成桌面应用。

## 2. 设计目标

- **轻量可用**：不引入独立向量数据库，MVP 阶段用 MySQL + NumPy 完成相似度检索。
- **隐私优先**：日记正文和索引数据都保存在本机，默认不依赖云数据库。
- **低门槛**：Embedding 和 LLM 优先使用免费 API，通过统一适配层隔离具体供应商。
- **可演进**：向量存储、Embedding、LLM 都做成接口，后续可以替换成本地模型或更重的向量库。
- **可维护**：索引任务增量、幂等、可重试，支持全量重建，不因一次失败卡死后续写入。

## 3. 功能设计（MVP）

### 3.1 写日记

- 用户输入纯文本，点击保存后新增一条日记。
- 保存时自动记录日期和时间（本地时间）。
- 日记正文以纯文本形式存储在 MySQL 的 `entries` 表中。
- 支持查看日记列表、按时间倒序展示、查看单篇正文、删除日记。

### 3.2 日记向量化

- 新日记保存后异步进入索引流程。
- 每条日记按规则切分为合适大小的 chunk，超短日记可以不切分。
- 调用 Embedding API 将每个 chunk 转成向量。
- chunk 原文与向量一起写入本地 MySQL，向量使用 `BLOB` 存储。
- 索引过程支持增量、幂等、失败重试与全量重建。

### 3.3 AI 对话问答

- 用户输入问题后，系统先对问题做向量化。
- 在日记向量库中检索最相似的 chunk，得到候选段落及相似度分数。
- 将候选段落、日期信息和问题一起发给 LLM。
- LLM 基于检索到的上下文生成回答，前端同时展示引用来源。
- 当最高相似度低于阈值时，不硬编答案，回复“没有找到相关日记”或等价提示。

### 3.4 索引状态

- 展示日记条目数、chunk 数、向量记录数、Embedding 维度。
- 展示最近索引任务的状态：空闲、处理中、成功、失败。
- 提供触发全量重建的入口。

## 4. 核心流程

### 4.1 保存与索引

```text
前端输入日记
  → POST /api/entries 保存正文和时间
  → 异步索引任务切入
  → 切分 chunk
  → Embedding API 生成向量
  → 写入 fragments / vectors
  → 索引状态更新
```

### 4.2 提问与回答

```text
用户输入问题
  → 问题向量化
  → 向量库 Top-K 检索
  → 相似度低于阈值？
      → 是：返回“没有找到相关日记”
      → 否：组装上下文 + 问题
  → LLM 生成回答
  → 返回回答与引用来源
```

## 5. 模块划分

| 模块 | 职责 |
| --- | --- |
| 前端页面 | 写日记、日记列表、AI 问答、索引状态 |
| 前端 API 层 | 与 FastAPI 交互，替换 demo 阶段的 mock 数据 |
| 后端 API | 日记 CRUD、AI 问答、索引控制 |
| 数据访问层 | SQLAlchemy 读写 MySQL |
| 文本切分 | 按字符数/段落切分日记，生成带顺序的 chunk |
| Embedding 适配层 | 统一接入 OpenAI-compatible 的免费 Embedding API |
| 向量存储 | MySQL + NumPy 余弦相似度；以接口隔离 |
| LLM 适配层 | 统一接入 OpenAI-compatible 的免费 Chat API |
| 索引任务 | 增量索引、失败重试、全量重建 |

## 6. 系统边界

- MVP 不引入独立向量数据库，向量以 `BLOB` 存在 MySQL。
- MVP 不做多用户、登录鉴权和云同步。
- 日记正文是唯一事实来源，chunk 与向量均可从正文重建。
- 删除日记时级联删除对应 chunk 和向量。
- 问题检索发生在**本地向量库**，只有向量化和文本补全请求会出网。

## 7. 概念数据模型

| 对象 | 关键字段 | 说明 |
| --- | --- | --- |
| 日记条目 Entry | id、content、created_at | 用户写的纯文本日记 |
| 文本片段 Chunk | id、entry_id、seq、content | 单篇日记切分后的段落 |
| 向量记录 Vector | id、chunk_id、embedding、dim、model | 某个模型的向量结果 |
| 索引任务 IndexJob | id、type、status、progress、error | 记录索引进度和失败原因 |

详细表结构和字段约束见 `TECH_DESIGN.md`。

## 8. 接口设计（概要）

- `POST /api/entries`：保存日记。
- `GET /api/entries`：分页读取日记。
- `DELETE /api/entries/{id}`：删除日记并清理索引。
- `POST /api/ask`：提问，返回回答、来源和相似度。
- `GET /api/search`：低层检索接口，返回命中的 chunk。
- `GET /api/index/status`：查看索引任务状态。
- `POST /api/index/rebuild`：触发全量重建。

## 9. 验收标准

1. 用户能保存一篇短文，列表中立即出现带时间戳的新日记。
2. 保存后的日记自动进入索引流程，索引状态能反映成功或失败。
3. 对已索引日记提问，能返回与内容相关的回答，并展示引用来源。
4. 提问与所有日记都不相关时，返回“没有找到相关日记”，不编造内容。
5. 删除日记后，相应 chunk 和向量同步删除，不再出现在检索结果中。
6. 索引失败不影响继续写日记，重建索引可以恢复完整数据。
7. 前后端联调时，用户操作在本地网页中端到端可用。

## 10. 里程碑

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| M0 | 纯前端 demo，验证页面形态和交互 | 已完成 |
| M1 | FastAPI + MySQL 数据模型，完成日记 CRUD | 待做 |
| M2 | 文本切分、Embedding、向量入库与检索 | 待做 |
| M3 | RAG 问答链路、相似度阈值与引用来源 | 待做 |
| M4 | 索引任务、异常处理、全量重建与体验打磨 | 待做 |
| M5 | 打包为本地桌面应用（可选） | 后续 |

## 11. 待讨论项

- Embedding 与 LLM 最终选型：优先免费 API，之后再根据额度、效果和稳定性确定。
- 各自定义化具体模型后，向量的维度与相似度阈值需要实测调整。
- MySQL 后续是否使用原生向量类型，取决于本机版本支持和检索规模。
- 桌面打包方案：Tauri 或 Electron，等 web 版本稳定后再评估。
- 是否增加日记日期筛选、全文检索兜底、本地只读 LLM 等进阶功能。
